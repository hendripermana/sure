---
en:
  transactions:
    form:
      account: Account
      account_prompt: Select an Account
      amount: Amount
      category: Category
      category_prompt: Select a Category
      add_split_row_button: Add row
      minimum_splits_error: At least 2 splits are required.
      remaining_balance_balanced: Balanced
      remaining_balance_label: Remaining
      remaining_balance_unbalanced: Unbalanced
      remove_split_row_button: Remove split
      split_amounts_error: Split amounts must equal the total transaction amount.
      split_amounts_unbalanced_message: Adjust the amounts above so the remaining balance is zero.
      split_row_amount_label: Amount
      split_row_amount_placeholder: '0.00'
      split_row_category_label: Category
      split_row_category_prompt: Select category
      split_row_name_label: Name
      split_row_name_placeholder: Split name
      split_transaction_description: Divide this transaction into multiple categories or parts.
      split_transaction_label: Split Transaction
      transaction_error: We couldn't save this transaction. Please review the errors and try again.
      date: Date
      description: Description
      description_placeholder: Describe transaction
      expense: Expense
      income: Income
      none: (none)
      note_label: Notes
      note_placeholder: Enter a note
      submit: Add transaction
      tags_label: Tags
      transfer: Transfer
    new:
      new_transaction: New transaction
    split_group:
      across_categories:
        one: across 1 category
        other: across %{count} categories
      edit_splits: Edit splits
      excluded_tooltip: Excluded from balance — counted across its splits
      parts:
        one: 1 part
        other: "%{count} parts"
      split_badge: Split
      unsplit: Unsplit
      unsplit_confirm: Remove the split and restore the original transaction?
    show:
      account_label: Account
      amount: Amount
      category_label: Category
      date_label: Date
      delete: Delete
      delete_subtitle: This permanently deletes the transaction, affects your historical
        balances, and cannot be undone.
      delete_title: Delete transaction
      details: Details
      merchant_label: Merchant
      name_label: Name
      nature: Type
      none: "(none)"
      note_label: Notes
      note_placeholder: Enter a note
      overview: Overview
      settings: Settings
      tags_label: Tags
      uncategorized: "(uncategorized)"
      recurring_title: Recurring Transaction
      recurring_subtitle: Mark this transaction as recurring to track it automatically.
      mark_as_recurring: Mark as Recurring
    header:
      edit_categories: Edit categories
      edit_imports: Edit imports
      edit_merchants: Edit merchants
      edit_tags: Edit tags
      import: Import
    index:
      transaction: transaction
      transactions: transactions
    searches:
      filters:
        amount_filter:
          equal_to: Equal to
          greater_than: Greater than
          less_than: Less than
          placeholder: '0'
        badge:
          expense: Expense
          income: Income
          on_or_after: on or after %{date}
          on_or_before: on or before %{date}
          transfer: Transfer
        type_filter:
          expense: Expense
          income: Income
          transfer: Transfer
      menu:
        account_filter: Account
        amount_filter: Amount
        apply: Apply
        cancel: Cancel
        category_filter: Category
        clear_filters: Clear filters
        date_filter: Date
        merchant_filter: Merchant
        tag_filter: Tag
        type_filter: Type
      search:
        equal_to: equal to
        greater_than: greater than
        less_than: less than
